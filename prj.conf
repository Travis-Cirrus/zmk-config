# Enable RGB underglow support
CONFIG_ZMK_RGB_UNDERGLOW=y

# Enable WS2812 RGB LED driver (use this for addressable LEDs like WS2812)
CONFIG_WS2812_STRIP=y

# Set the GPIO driver for controlling LEDs
CONFIG_GPIO=y

# Enable PWM (Pulse Width Modulation) to control LED brightness
CONFIG_PWM=y